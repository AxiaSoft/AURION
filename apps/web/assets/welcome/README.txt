Welcome videos — drop your files here (MP4 preferred, WebM optional).

Desktop (width over 820px):
  desktop.mp4
  desktop.webm          (optional)
  intro-desktop.mp4     (optional fallback)

Phone / tablet (width 820px and below):
  mobile.mp4
  mobile.webm           (optional)
  intro-mobile.mp4      (optional fallback)

Shared fallbacks if a size-specific file is missing:
  intro.mp4
  intro.webm
  welcome.mp4

AURION plays the matching file with sound when the desk opens,
then fades into the dashboard. No logo overlay.
If the browser blocks autoplay-with-sound, tap the screen (or the
speaker button) once — the video stays on screen and audio starts.
Skip is always available.
