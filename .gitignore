# Runtime artifacts — never committed
node_modules/
__pycache__/
*.pyc
*.log
dist/
.venv/

# AURION live state
data/license/
data/logs/
data/ea-inbox/
data/settings-backup.json
engine/models/

# Key server (store) local state + secrets
store/keyserver/data/
store/keyserver/.env

# Telegram bot token — provisioned locally in the source, never committed.
# Only the .example template is tracked.
config/telegram.token
config/telegram.local.json
admin/telegram-panel/panel.token

# Generated economic-calendar cache (refetched from the Forex Factory feed)
config/news_calendar.csv
config/news_calendar.fetched

# Update server local state + secrets
admin/update-server/data/
admin/update-server/.env

# Owner key ledger
data/license/issued.json

# Generated desk artifacts (jwt secret, exports) - regenerated at first run.
# NOTE: data/jwt.secret and data/users.json are still tracked by git in this
# branch; run "git rm --cached data/jwt.secret data/users.json" to untrack them.
data/jwt.secret
data/users.json
data/exports/

.env

# OS/editor noise
.DS_Store
Thumbs.db
